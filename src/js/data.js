let stopSort = false;
let toggle = false;

const changeStopSort = (value) => {
  stopSort = value;
};

const changeToggle = (value) => {
  toggle = value;
};

export { stopSort, toggle, changeToggle, changeStopSort };
